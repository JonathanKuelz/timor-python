# content of pytest.ini
[pytest]
markers =
    imports: marks a test that only checks imports and must be run in an independant suite.
    ompl: mark a test which should be run with ompl in container.
    render: mark a test which should be run with node + puppeteer + chromium (or sim.) installed.
    sequential: mark a test which needs to be run alone without parallel tests.
